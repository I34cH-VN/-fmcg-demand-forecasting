# Raw Data

Place the source CSV files in this folder before running the pipeline.

Expected files:

- `data_2023.csv`
- `data_2024.csv`
- `data_2025.csv`

The raw shipment data is excluded from Git by default because it may contain private business information.
